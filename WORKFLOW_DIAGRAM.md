# Project Workflow Diagram

## Branch Structure and Deployment Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         BRANCH STRUCTURE                         │
└─────────────────────────────────────────────────────────────────┘

                         ┌──────────────┐
                         │   feature/   │
                         │   bugfix/    │◄───── Developers work here
                         └──────┬───────┘
                                │ PR + Review
                                ▼
                         ┌──────────────┐
                         │   develop    │◄───── Main development branch
                         │  (default)   │       (Protected)
                         └──────┬───────┘
                                │ PR + Review + Tests
                                ▼
                         ┌──────────────┐
                         │   staging    │◄───── Pre-production testing
                         │              │       (Protected)
                         └──────┬───────┘       Auto-deploys to staging
                                │ PR + Review + Staging Tests
                                ▼
                         ┌──────────────┐
                         │     main     │◄───── Production
                         │ (production) │       (Protected)
                         └──────────────┘       Auto-deploys to production


                         ┌──────────────┐
                         │   hotfix/    │◄───── Emergency fixes
                         └──────┬───────┘       Branch from main
                                │ Fast PR
                                ▼
                         Back to main, then merge to develop + staging


┌─────────────────────────────────────────────────────────────────┐
│                         CI/CD PIPELINE                           │
└─────────────────────────────────────────────────────────────────┘

  Push/PR to any branch
         │
         ▼
  ┌─────────────┐
  │   Trigger   │
  │  GitHub     │
  │  Actions    │
  └─────┬───────┘
        │
        ├────────────────────┬────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
  ┌──────────┐        ┌──────────┐        ┌──────────┐
  │   Test   │        │   Lint   │        │  Build   │
  │   Job    │        │   Job    │        │  (opt)   │
  └────┬─────┘        └────┬─────┘        └────┬─────┘
       │                   │                    │
       └────────┬──────────┴────────────────────┘
                │ All checks pass?
                ▼
         ┌─────────────┐
         │   Deploy?   │
         └──────┬──────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
  ┌──────────┐    ┌──────────┐
  │ Staging  │    │Production│
  │ Deploy   │    │  Deploy  │
  │(staging  │    │  (main   │
  │ branch)  │    │  branch) │
  └──────────┘    └──────────┘


┌─────────────────────────────────────────────────────────────────┐
│                    DEVELOPMENT WORKFLOW                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│ 1. Start Feature│
└────────┬────────┘
         │
         │ git checkout develop
         │ git checkout -b feature/new-feature
         ▼
┌─────────────────┐
│ 2. Development  │
└────────┬────────┘
         │
         │ Write code
         │ Write tests
         │ Test locally
         ▼
┌─────────────────┐
│ 3. Push & PR    │
└────────┬────────┘
         │
         │ git push origin feature/new-feature
         │ Create PR to develop
         ▼
┌─────────────────┐
│ 4. CI/CD Checks │
└────────┬────────┘
         │
         │ Tests run
         │ Linting runs
         ▼
┌─────────────────┐
│ 5. Code Review  │
└────────┬────────┘
         │
         │ Team reviews
         │ Address feedback
         ▼
┌─────────────────┐
│ 6. Merge        │
└────────┬────────┘
         │
         │ Merge to develop
         ▼
┌─────────────────┐
│ 7. Test Staging │
└────────┬────────┘
         │
         │ PR develop → staging
         │ Auto-deploy to staging
         │ Test on staging env
         ▼
┌─────────────────┐
│ 8. Production   │
└────────┬────────┘
         │
         │ PR staging → main
         │ Auto-deploy to prod
         │ Monitor
         ▼
┌─────────────────┐
│   Complete! ✓   │
└─────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                      PROTECTION RULES                            │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ main (Production)                                    │
├──────────────────────────────────────────────────────┤
│ ✓ Require 2 PR approvals                            │
│ ✓ Require status checks (test, lint)                │
│ ✓ Require up-to-date branch                         │
│ ✓ Require conversation resolution                   │
│ ✓ No direct pushes                                  │
│ ✓ No force pushes                                   │
│ ✓ No deletions                                      │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ staging (Pre-production)                             │
├──────────────────────────────────────────────────────┤
│ ✓ Require 1 PR approval                             │
│ ✓ Require status checks (test, lint)                │
│ ✓ Require up-to-date branch                         │
│ ✓ Require conversation resolution                   │
│ ✓ No direct pushes                                  │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ develop (Development)                                │
├──────────────────────────────────────────────────────┤
│ ✓ Require 1 PR approval                             │
│ ✓ Require status checks (test, lint)                │
│ ✓ Require conversation resolution                   │
│ ✓ No direct pushes                                  │
└──────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────┐
│                         FILE STRUCTURE                           │
└─────────────────────────────────────────────────────────────────┘

Lung-Disease-/
│
├── 📁 .github/
│   └── workflows/
│       └── ci-cd.yml              ← CI/CD pipeline
│
├── 📄 .env.example                ← Environment template
├── 📄 .gitignore                  ← Git ignore rules
│
├── 📘 Documentation
│   ├── SETUP_SUMMARY.md           ← Start here! Overview
│   ├── README.md                  ← Project docs
│   ├── BRANCHING_STRATEGY.md      ← Git workflow
│   ├── BRANCH_SETUP.md            ← Setup guide
│   ├── BRANCH_PROTECTION.md       ← Protection rules
│   ├── DEPLOYMENT.md              ← Deploy guides
│   ├── CONTRIBUTING.md            ← Contribution guide
│   └── WORKFLOW_DIAGRAM.md        ← This file
│
├── 🚀 Automation Scripts
│   ├── setup-branches.sh          ← Create branches
│   ├── deploy-staging.sh          ← Deploy staging
│   └── deploy-production.sh       ← Deploy production
│
├── 🐳 Docker Configuration
│   ├── Dockerfile                 ← Container config
│   └── docker-compose.yml         ← Multi-container
│
├── 📦 Python Configuration
│   └── requirements.txt           ← Dependencies
│
└── 📜 LICENSE                     ← Project license


┌─────────────────────────────────────────────────────────────────┐
│                     QUICK REFERENCE COMMANDS                     │
└─────────────────────────────────────────────────────────────────┘

# Setup (Repository Owner)
./setup-branches.sh                    # Create all branches

# Development
git checkout develop
git checkout -b feature/my-feature     # Start new feature
git push origin feature/my-feature     # Push feature
# Create PR on GitHub

# Deployment
./deploy-staging.sh                    # Manual staging deploy
./deploy-production.sh                 # Manual prod deploy
# Or let CI/CD auto-deploy on branch push

# Status Check
git branch -a                          # List all branches
git status                             # Check current status
git log --oneline --graph --all        # View branch history

# Docker
docker-compose up                      # Run with Docker
docker build -t lung-disease .         # Build image
docker run -p 8000:8000 lung-disease   # Run container


┌─────────────────────────────────────────────────────────────────┐
│                         SUCCESS METRICS                          │
└─────────────────────────────────────────────────────────────────┘

✓ All branches created (main, staging, develop)
✓ Branch protection rules configured
✓ CI/CD pipeline running
✓ Documentation complete
✓ Scripts executable and tested
✓ Docker configuration ready
✓ Zero direct commits to protected branches
✓ All changes go through PR review
✓ Automated testing on every PR
✓ Automated deployment on merge


┌─────────────────────────────────────────────────────────────────┐
│                      TROUBLESHOOTING GUIDE                       │
└─────────────────────────────────────────────────────────────────┘

Problem: CI/CD not running
└─► Check: Settings → Actions → Enable workflows

Problem: Can't push to branch
└─► Check: Branch protection rules active
    Solution: Create PR instead

Problem: Tests failing
└─► Check: GitHub Actions logs
    Solution: Fix code and push again

Problem: Merge conflicts
└─► Solution: Update branch with base
    git checkout feature/branch
    git merge develop
    # Resolve conflicts
    git push

Problem: Deployment fails
└─► Check: Secrets configured in GitHub
    Check: Deployment script logs
    Check: Platform-specific settings


┌─────────────────────────────────────────────────────────────────┐
│                         NEED HELP?                               │
└─────────────────────────────────────────────────────────────────┘

📖 Read the documentation in this repository
🔍 Check GitHub Actions logs for errors
💬 Create an issue with details
👥 Contact repository maintainers
🌐 Review external resources in docs

---
This workflow ensures professional, safe, and scalable development! 🚀
